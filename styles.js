import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  card: {
    flex: 1,
    position: "absolute"
  },
  container: {
    alignItems: "stretch"
  }
})

export default styles
